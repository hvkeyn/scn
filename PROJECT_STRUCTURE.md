# Структура проекта SCN

## Основные директории

### ✅ Активные (используются)

- **`scn/`** - Основной проект SCN
  - Весь код приложения
  - Flutter/Dart проект
  - Единственная необходимая директория для разработки

- **`scn-release/`** - Папка с готовой сборкой
  - Создается автоматически при сборке
  - Содержит исполняемые файлы и ресурсы

- **`build.ps1`** - Скрипт сборки проекта
  - Поддерживает сборку упрощенной версии
  - Использование: `.\work.ps1 -Project simple -Platform windows -BuildType zip`

- **`submodules/flutter/`** - Flutter SDK (если используется как submodule)
  - Необходим для сборки проекта

- **`README.md`** - Основная документация проекта

### 📦 Оригинальный проект (не используется в упрощенной версии)

Эти директории относятся к оригинальному LocalSend проекту и не нужны для работы упрощенной версии:

- **`app/`** - Оригинальный Flutter проект с Rust зависимостями
- **`core/`** - Rust core библиотека
- **`server/`** - Rust HTTP сервер
- **`common/`** - Общий код для оригинального проекта
- **`cli/`** - CLI версия приложения
- **`scripts/`** - Скрипты сборки оригинального проекта
- **`readme_i18n/`** - Переводы README для оригинального проекта
- **`fastlane/`** - Конфигурация для магазинов приложений
- **`msix/`** - MSIX пакет для Windows Store

### 📝 Документация и конфигурация

- **`memory-bank/`** - Документация проекта (можно оставить для справки)
- **`SIMPLIFIED_PLAN.md`** - План упрощения (исторический документ)
- **`BUILD.md`** - Инструкции по сборке (для оригинального проекта)
- **`CONTRIBUTING.md`** - Руководство по вкладу (для оригинального проекта)
- **`CHANGELOG.md`** - История изменений

## Минимальная структура для работы

Для работы с упрощенной версией SCN достаточно:

```
scn/
├── scn/                 # Основной проект
├── build.ps1            # Скрипт сборки
├── README.md            # Документация
└── scn-release/         # Готовая сборка (создается автоматически)
```

Все остальные директории можно удалить, если они не нужны.

## Рекомендации

1. **Для разработки:** Используйте только `scn/`
2. **Для сборки:** Используйте `build.ps1` с параметром `-Project scn`
3. **Для релиза:** Результат в `scn-release/`

## Очистка проекта

Если хотите оставить только упрощенную версию, можно удалить:

- `app/` - оригинальный проект
- `core/` - Rust core
- `server/` - Rust server
- `common/` - общий код
- `cli/` - CLI версия
- `scripts/` - скрипты оригинального проекта
- `readme_i18n/` - переводы README
- `fastlane/` - конфигурация магазинов
- `msix/` - MSIX пакет
- `SIMPLIFIED_PLAN.md` - исторический документ

**Внимание:** Удаляйте только если уверены, что они не нужны!

